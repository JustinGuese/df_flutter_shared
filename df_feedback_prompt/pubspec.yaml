name: df_feedback_prompt
description: Reusable first-launch feedback prompt dialog with Riverpod configuration.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  shared_preferences: ^2.3.2

